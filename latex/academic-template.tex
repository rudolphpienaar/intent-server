\documentclass[$if(classoption)$$for(classoption)$$classoption$$sep$,$endfor$$else$conference$endif$]{IEEEtran}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{enumitem}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}
\setlist{leftmargin=*}
\setlength{\columnsep}{18pt}

\title{$title$$if(subtitle)$\\[0.25em]\large $subtitle$$endif$}
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
$if(affiliation)$
\date{$for(affiliation)$$affiliation$$sep$ \\ $endfor$\\$if(date)$$date$$else$\today$endif$}
$else$
$if(date)$
\date{$date$}
$else$
\date{\today}
$endif$
$endif$

\begin{document}

\maketitle

$body$

\end{document}
